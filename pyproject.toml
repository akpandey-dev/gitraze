[project]
name = "gitraze"
version = "0.0.1"
description = "GitHub reconnaissance and search toolkit"
authors = [{ name = "AK" }]
dependencies = []
readme = "README.md"
requires-python = ">=3.8"

[project.scripts]
gitraze = "gitraze.cli:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"